BoxLayout:
    orientation: 'vertical'
#    orientation: 'horizontal'
    Button:
        text: 'Clear'
        on_press: app.clear_all()
    TextInput:
        id: input_name
        text: ''
        multiline: False
        # This event is when the user enters text (and presses Enter)
        on_text_validate: app.handle_greet()
    Button:
        text: 'three'
    Label:
        id: output_label
        color: (1, 1, 0, 1)
    Button:
        text: "Press me"
        on_press: app.handle_greet()